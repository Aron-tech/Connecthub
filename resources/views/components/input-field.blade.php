<div class="mb-6">
    <label for="{{ $id }}" class="block text-sm font-medium text-gray-700">{{ $label }}</label>
    <input type="{{ $type }}" id="{{ $id }}" name="{{ $name }}" class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm h-12 text-lg p-2 focus:border-purple-600 focus:ring-1 focus:ring-purple-600" {{ $attributes }}>
</div>
